/*
package brightone.pl.zadanie.newProject;

public class Game {
    Player white = new Player(1);
    Player black = new Player(2);

    Board board = new Board();

    public  void refresh(Figure figure, int playerID){
        for (int i = 0; i < 15; i++) {
            if (playerID==1){
                if (((white.allFigures[i].x) == figure.x) && (white.allFigures[i].y) == figure.y ){
                    white.allFigures[i]= null;
                }
            }
            if (playerID==2){
                if (((black.allFigures[i].x) == figure.x) && (black.allFigures[i].y) == figure.y) {
                    black.allFigures[i]= null;
                }
            }
        }

    }
}
*/
